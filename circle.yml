## Customize the test machine
machine:
  xcode:
    version: 9.2.0

  timezone:
    America/New_York # Set the timezone

## Customize test commands
test:
  post:
    - xcodebuild test -sdk macosx10.13 -project Atlas.xcodeproj/ -scheme Atlas -destination 'platform=OS X,arch=x86_64'
    
